# Database - SQLite (zero config, no setup)
# For local dev, use the default below. For PostgreSQL: postgresql://user:password@localhost:5432/cofounderbay
DATABASE_URL="file:./dev.db"

# JWT secret - generate a strong random string for production
JWT_SECRET="your-super-secret-jwt-key-change-in-production"

# Server
PORT=3001
